{
  "name": "degreepilot",
  "private": true,
  "version": "0.1.0",
  "description": "DegreePilot — agentic academic planning platform (MVP: Columbia CS + Econ).",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint"
  }
}
