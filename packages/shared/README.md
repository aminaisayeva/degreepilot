# @degreepilot/shared

Shared TypeScript types mirroring the FastAPI schemas in [apps/api](../../apps/api).
Used as a single source of truth between the API and any future TypeScript
consumers. For zero-friction local dev, the web app ships a local copy under
`apps/web/src/types/api.ts`.
